// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: platform.sql

package db

import (
	"context"
	"time"
)

const getPlatformOperatorByID = `-- name: GetPlatformOperatorByID :one
select id, username, name, is_active
from platform_operators
where id = $1
`

type GetPlatformOperatorByIDRow struct {
	ID       int64  `json:"id"`
	Username string `json:"username"`
	Name     string `json:"name"`
	IsActive bool   `json:"is_active"`
}

// La usa el middleware en CADA request: retirarle el acceso a un operador tiene que morder en el
// siguiente, no cuando caduque su token. Es una lectura por PK sobre una tabla de una o dos filas.
func (q *Queries) GetPlatformOperatorByID(ctx context.Context, id int64) (GetPlatformOperatorByIDRow, error) {
	row := q.db.QueryRow(ctx, getPlatformOperatorByID, id)
	var i GetPlatformOperatorByIDRow
	err := row.Scan(
		&i.ID,
		&i.Username,
		&i.Name,
		&i.IsActive,
	)
	return i, err
}

const getPlatformOperatorByUsername = `-- name: GetPlatformOperatorByUsername :one

select id, username, name, password_hash, is_active
from platform_operators
where username = $1
`

type GetPlatformOperatorByUsernameRow struct {
	ID           int64  `json:"id"`
	Username     string `json:"username"`
	Name         string `json:"name"`
	PasswordHash string `json:"password_hash"`
	IsActive     bool   `json:"is_active"`
}

// Consultas de la CONSOLA DE PLATAFORMA (spec 016).
//
// Todas corren sobre la conexión del rol `gatobobah_platform`, que solo tiene `select` sobre
// `companies` y `platform_operators`. Si alguien agrega aquí una consulta sobre una tabla de
// operación, no va a compilar mal ni a devolver datos de más: va a fallar con 42501 en el primer
// request, que es exactamente lo que se quiere.
// El login de la consola. Trae también `is_active` en vez de filtrarlo en el WHERE: el servicio
// necesita distinguir "no existe" de "está apagado" para gastar el mismo tiempo en los dos casos,
// y responder lo mismo. Filtrarlo aquí haría que la rama del apagado saliera en microsegundos.
func (q *Queries) GetPlatformOperatorByUsername(ctx context.Context, username string) (GetPlatformOperatorByUsernameRow, error) {
	row := q.db.QueryRow(ctx, getPlatformOperatorByUsername, username)
	var i GetPlatformOperatorByUsernameRow
	err := row.Scan(
		&i.ID,
		&i.Username,
		&i.Name,
		&i.PasswordHash,
		&i.IsActive,
	)
	return i, err
}

const listCompaniesForPlatform = `-- name: ListCompaniesForPlatform :many
select id, slug, name, is_active, created_at
from companies
order by name
`

type ListCompaniesForPlatformRow struct {
	ID        int64     `json:"id"`
	Slug      string    `json:"slug"`
	Name      string    `json:"name"`
	IsActive  bool      `json:"is_active"`
	CreatedAt time.Time `json:"created_at"`
}

// El catálogo de clientes. Ni una cifra de dinero (FR-008) y ni un dato de los empleados de un
// cliente (FR-016): lo que no se selecciona no se puede filtrar mal después.
//
// Ve TODAS las empresas gracias a la política `plataforma_lee_todas_las_empresas`; el `select` a
// secas devolvería una sola, porque `companies` lleva RLS y también le aplica a este rol.
func (q *Queries) ListCompaniesForPlatform(ctx context.Context) ([]ListCompaniesForPlatformRow, error) {
	rows, err := q.db.Query(ctx, listCompaniesForPlatform)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListCompaniesForPlatformRow{}
	for rows.Next() {
		var i ListCompaniesForPlatformRow
		if err := rows.Scan(
			&i.ID,
			&i.Slug,
			&i.Name,
			&i.IsActive,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertPlatformOperator = `-- name: UpsertPlatformOperator :one
insert into platform_operators (username, name, password_hash)
values ($1, $2, $3)
on conflict (username) do update
  set name = excluded.name,
      password_hash = excluded.password_hash,
      is_active = true,
      updated_at = now()
returning id
`

type UpsertPlatformOperatorParams struct {
	Username     string `json:"username"`
	Name         string `json:"name"`
	PasswordHash string `json:"password_hash"`
}

// La escribe el DUEÑO de la base desde la bandera del binario, nunca el rol de la consola: éste no
// tiene `insert` ni `update` sobre su propia tabla, y esa omisión es deliberada.
//
// Reactiva al operador si estaba apagado, porque es una herramienta de recuperación: quien corre
// esto tiene acceso al servidor y a las variables de entorno, y lo que quiere es volver a entrar.
func (q *Queries) UpsertPlatformOperator(ctx context.Context, arg UpsertPlatformOperatorParams) (int64, error) {
	row := q.db.QueryRow(ctx, upsertPlatformOperator, arg.Username, arg.Name, arg.PasswordHash)
	var id int64
	err := row.Scan(&id)
	return id, err
}
